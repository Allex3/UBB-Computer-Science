valley([A, B, C | T]):-
    A > B,
    valley([B, C | T]).
valley([A, B, C | T]):-
    A > B,
    B < C,
    valleyUp([C| T]).
valleyUp([]).
valleyUp([_]).
valleyUp([A,B | T]):-
    A < B,
    valleyUp([B|T]).


alternateSumPlus([],0).
alternateSumPlus([X|T],S):-
    alternateSumMinus(T, NewS),
    S is X + NewS.
alternateSumMinus([],0).
alternateSumMinus([X|T],S):-
    alternateSumPlus(T, newS),
    S is -X + NewS.
